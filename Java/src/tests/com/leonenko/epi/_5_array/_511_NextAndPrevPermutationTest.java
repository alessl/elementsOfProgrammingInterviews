package com.leonenko.epi._5_array;

import org.junit.jupiter.api.Test;

class _511_NextAndPrevPermutationTest {

    @Test
    void testGetPrevPerm() {

    }

    @Test
    void testGetNextPerm() {

    }

//    public static void main(String[] args) {
//        printNextPerm(new int[] {1, 2, 3, 4, 5, 6, 7});
//    }

    /*private static void printNextPerm(int[] array) {
        int[] perm = _512_SampleOfflineData.sample(array, array.length);
        System.out.println("Perm: " + Arrays.toString(perm));

        int[] nextPerm = getNextPerm(Arrays.copyOf(perm, perm.length));
        System.out.println("Next: " + Arrays.toString(nextPerm));

        int[] prevPerm = getPrevPerm(Arrays.copyOf(perm, perm.length));
        System.out.println("Prev: " + Arrays.toString(prevPerm));
    }*/
}